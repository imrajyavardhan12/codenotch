.DS_Store
build/
DerivedData/
*.xcodeproj
*.xcworkspace
!*.xcworkspace/contents.xcworkspacedata
xcuserdata/
*.xcuserstate
.swiftpm/

# Release artifacts are published via GitHub Releases, not git — a .dmg in
# the tree bloats every future clone. The Sparkle appcast in site/ stays
# tracked; the disk image it points at does not.
site/*.dmg
